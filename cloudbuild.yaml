steps:
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - gcloud
      - beta
      - functions
      - deploy
      - main
      - --region=europe-west2
      - --source=.
      - --trigger-http
      - --runtime=python310
      - --allow-unauthenticated